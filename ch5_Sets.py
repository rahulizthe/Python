'''Sets - ordered collection of unique elements.
   basically, a dictionary without keys.
   also, there is no repetition of elements is allowed in sets.
   sets are unindexed.'''

s = {1,2,3,4}
print(s, type(s))

#to make an empty set
a =  set()
print(type(a))

